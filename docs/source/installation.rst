Installation
============

Requirements
------------

The TFBM package requires the following dependencies:

* Python >= 3.6
* NumPy
* SciPy
* mpmath

Installing from Source
----------------------

You can install the package directly from the source code:

.. code-block:: bash

   git clone https://github.com/Malinon/tfbm
   cd tfbm
   pip install .
